<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %> - Blog Website</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General Styling */
        body {
            background: linear-gradient(135deg, #f9f9f9, #e0eafc);
            font-family: 'Roboto', sans-serif;
            color: #333;
            margin: 0;
            padding: 0;
        }

        /* Navbar Styling */
        .navbar {
            background: #006d77;
            padding: 10px 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-size: 1.8rem;
            font-weight: bold;
            color: #fff;
        }

        .navbar-text {
            color: #e0f7fa;
            margin-left: auto;
            font-size: 1rem;
        }

        /* Blog Post Styling */
        .blog-content {
            margin-top: 50px;
        }

        .blog-title {
            font-size: 2.5rem;
            font-weight: bold;
            color: #006d77;
        }

        .blog-author {
            font-size: 1.2rem;
            color: #e63946;
            margin-bottom: 20px;
        }

        .blog-body {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 40px;
        }

        /* Blog Image Styling */
        .blog-image {
            width: 100%;
            max-height: 500px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 20px;
        }

        .back-btn {
            background-color: #006d77;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .back-btn:hover {
            background-color: #005f5f;
        }

    </style>
</head>
<body>
   

    <!-- Blog Post Content -->
    <div class="container blog-content">
        <!-- Blog Image -->
        <img src="<%= blog.imageUrl %>" alt="Blog Image" class="blog-image">
        
        <!-- Blog Title -->
        <h2 class="blog-title"><%= blog.title %></h2>

        <!-- Blog Author -->
        <p class="blog-author">By <%= blog.author %></p>

        <!-- Blog Content -->
        <div class="blog-body">
            <p><%= blog.content %></p>
        </div>

        <!-- Back Button -->
        <!-- <a href="/home" class="back-btn">Back to Blog List</a> -->
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
